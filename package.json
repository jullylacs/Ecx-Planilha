{
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACK,FRONT\" -c \"bgBlue.bold,bgGreen.bold\" --kill-others-on-fail \"npm --prefix BackEnd run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix BackEnd run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "db:migrate": "npm --prefix BackEnd run db:migrate"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
